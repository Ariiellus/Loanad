# Security configuration for backend npm packages
# Minimum release age in days (default: 0, meaning no minimum age)
audit-level=moderate

# Exclude specific packages from minimum release age requirements
# This allows critical packages to be installed even if they're very new
npmMinimumReleaseAgeExclude=express,ethers,cors,dotenv,nodemon,node

# Additional security settings
fund=false
audit=true
save-exact=false
